server:
  port: 7900
spring:
  application:
    name: provider      # 注册到eureka后可以通过此名称调用服务
eureka:
  instance:
    prefer-ip-address: true  #在浏览器中优先显示ip
  client:
    service-url:
       defaultZone: http://eureka01:8888/eureka/,http://eureka02:8882/eureka/,http://eureka03:8883/eureka/   # 注册到eurekasever的地址
